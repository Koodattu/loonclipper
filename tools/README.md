# Runtime tools

Place these Windows executables in this directory before building or publishing:

- `yt-dlp.exe`
- `ffmpeg.exe`
- `ffprobe.exe`

`ffprobe.exe` is normally included in the same FFmpeg archive as `ffmpeg.exe`.

The project copies these files into a `tools` directory beside `LoonClipper.exe`.
